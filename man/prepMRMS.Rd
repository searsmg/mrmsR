% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepMRMS.R
\name{prepMRMS}
\alias{prepMRMS}
\title{Project and clip MRMS GRIB2 files using terra and furrr}
\usage{
prepMRMS(dir, output_dir, num_cores, boundary)
}
\arguments{
\item{dir}{Character. Directory containing MRMS \code{.grib2} files.}

\item{output_dir}{Character. Directory where processed \code{.tif} files will be saved.}

\item{num_cores}{Integer. Number of cores to use for parallel processing.}

\item{boundary}{Character. File path to the boundary (e.g., a shapefile)
defining the spatial extent to crop to. A path is required because
\code{SpatVector} objects can't be sent to parallel workers.}
}
\value{
Invisibly returns \code{NULL}. Writes processed raster files to \code{output_dir}.
}
\description{
Reads MRMS GRIB2 files, assigns a coordinate reference system (CRS),
crops them to a specified boundary, and writes the results as tif files.
Processing is performed in parallel using furrr.
}
\details{
Uses the \code{terra} package for raster processing and \code{furrr} for parallel execution.
The CRS is WGS84 (EPSG:4326).
}
\examples{
\dontrun{
prepMRMS(
  dir = "path/to/grib2_files",
  output_dir = "path/to/output",
  num_cores = 4,
  boundary = "path/to/boundary.shp"
)
}

}
