% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accumulateMRMS.R
\name{accumulateMRMS}
\alias{accumulateMRMS}
\title{Accumulate MRMS precipitation into totals}
\usage{
accumulateMRMS(
  data,
  product,
  by = c("day", "hour", "total"),
  tz = "UTC",
  value_col = "p_mmhr"
)
}
\arguments{
\item{data}{Data frame of per-timestep values, typically from
\code{\link[=combineCSV]{combineCSV()}}. Must have \code{catchment}, \code{datetime} (POSIXct), and the
column named by \code{value_col}.}

\item{product}{Character. The MRMS product the values came from; sets how
each value is converted to a depth:
\itemize{
\item \code{"SurfacePrecipRate"}: a rate in mm/hr every 2 minutes, so
each value contributes \code{rate * 2 / 60} mm
\item \code{"RadarOnlyQPE"}, \code{"MultiSensorQPE"}: already a depth
in mm for the hour, used as is
}}

\item{by}{Character. Period to total over: \code{"hour"}, \code{"day"} (default), or
\code{"total"} for the whole record.}

\item{tz}{Character. Time zone that defines hour and day boundaries
(default \code{"UTC"}). For example, use \code{"America/Denver"} for local days.}

\item{value_col}{Character. Name of the column holding the MRMS values
(default \code{"p_mmhr"}, the column \code{\link[=zonalMRMS]{zonalMRMS()}} writes).}
}
\value{
A data frame with one row per catchment and period:
\describe{
\item{catchment}{Catchment ID.}
\item{period_start}{Start of the period in \code{tz}; for \code{by = "total"},
the start of the first timestep.}
\item{depth_mm}{Total precipitation depth in mm.}
\item{n_steps}{Number of non-missing timesteps included.}
\item{expected_steps}{Number of timesteps a complete period has
(\code{NA} for \code{by = "total"}). Fewer \code{n_steps} means missing data, so
\code{depth_mm} may be an underestimate.}
}
}
\description{
Converts per-timestep MRMS values into precipitation depths and sums them
for each catchment by hour, day, or over the whole record.
}
\details{
Each MRMS timestamp marks the end of the interval it covers. For example,
the hourly QPE file stamped 01:00 covers 00:00 to 01:00, and the one
stamped 00:00 is counted in the previous day. Values are assigned to
periods accordingly.

Missing (\code{NA}) values are skipped and not counted in \code{n_steps}.
}
\examples{
\dontrun{
combined <- combineCSV("path/to/csv_dir")
daily <- accumulateMRMS(combined, product = "RadarOnlyQPE", by = "day")
}

}
